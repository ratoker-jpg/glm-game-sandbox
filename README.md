# Four Elements Remake - GLM Sandbox Copy

> **🎮 Live Demo (GitHub Pages):** https://ratoker-jpg.github.io/glm-game-sandbox/

This folder is an isolated sandbox repository for GLM work.

Sandbox path:

```text
C:\Users\Den\Desktop\GLM_test\glm_game_sandbox
```

Production path that must not be touched:

```text
C:\Users\Den\Desktop\four elements\four_elements_core_base
```

Run locally from the sandbox root:

```bat
00_START_GAME_WORK_8010.bat
```

Local URL:

```text
http://localhost:8010/index.html
```

Safe baseline check:

```bat
node --check src/main.js
```

Important:

- this sandbox must not sync to Google Drive;
- production workflow docs do not apply here as the source of truth;
- use `README_GLM_SANDBOX.md` and `AGENTS.md` for sandbox rules.
